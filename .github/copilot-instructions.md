# Copilot instructions

The canonical instructions for this repository live in [`AGENTS.md`](../AGENTS.md). Read that file
and follow it.

This shim exists only because Copilot looks here. Do not add guidance to this file - it would
drift from `AGENTS.md`, and `npm run doctor` reports shim drift as a gap.