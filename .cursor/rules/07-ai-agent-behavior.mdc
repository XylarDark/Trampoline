---
name: "AI Agent Behavior Rules"
description: "Meta-rules for AI agent behavior, tool usage, session cleanup, and error recording"
alwaysApply: true
priority: 9
version: 1.1
---

# AI Agent Behavior Rules

## Conversation and context

- **New logical task:** Prefer a new chat for a new unit of work so the agent stays focused. Bring in only the prior context you need (e.g. `@file`, past summary) instead of very long threads.
- **Long threads:** If context becomes noisy or contradictory, summarize progress and start a fresh chat with that summary as the first message.

## Session Continuity (optional)

If the project has a session or progress log (e.g. `docs/SESSION_LOG.md`, `docs/workflow/DAILY_STATE.md`):

- **At task start:** Read it to load context from prior sessions (completed work, blockers, errors, key decisions).
- **At task end:** Append a summary entry (date, tasks completed/remaining, errors encountered, key decisions).

Projects may define their own paths; adapt to the project's conventions.

## Context Window Awareness

Manage context proactively to stay effective on long-horizon tasks:

- **Targeted reads over full dumps:** Read specific line ranges or use semantic search instead of dumping entire large files.
- **Search strategy:** Use `Grep` for exact symbol/string lookup; use `SemanticSearch` for exploratory "how/where" questions; use `Glob` for finding files by name pattern.
- **Subtask decomposition:** Break large tasks into smaller subtasks. Complete and report on each subtask before starting the next.
- **Avoid context pollution:** Don't read files you don't need. Don't re-read files whose content you already have in context.

## Tool Usage

- Use `@file` references for context, not full file dumps
- Read files before editing (don't assume structure)
- Use semantic search for understanding codebase
- Batch related operations when possible
- Verify changes with linter/tests
- **Avoid PowerShell output limiting in terminal commands** - never use `Select-Object -First N` as it causes VPN/network issues
  - Prefer direct commands or command-specific flags for output control
  - Accept full output - modern terminals handle large output well

## When to Ask for Confirmation

- Breaking changes
- Large refactorings (> 10 files)
- Security-sensitive changes
- Changes to core architecture
- When user intent is unclear

## Code Generation

- Generate complete, working code (no TODOs)
- Include error handling
- Add comments for complex logic
- Follow project patterns
- Verify code compiles/runs
- **Verifiable goals:** For feature work, prefer defining or running tests as the success criterion; iterate until tests pass or document why tests are deferred.

## File Management

- Read files before editing
- Preserve existing structure when possible
- Update related files (tests, docs)
- Don't create unnecessary files

## Session Cleanup (mandatory before ending a task)

Temporary files created during a session **must be deleted** before the task is considered complete. This includes:

1. **Diagnostic/result files** in the project root (e.g. `*.json` result dumps, one-off reports) that are not project artifacts.
2. **One-off scripts** created for debugging, diagnostics, or one-time fixes. Keep only reusable/idempotent scripts referenced by orchestrators or docs.
3. **Temporary log or dump files** created for intermediate inspection.

**When to clean up:** After the task's objectives are verified and before reporting results to the user. Do not leave cleanup for a future session.

## Communication

- Explain approach before implementing
- Show reasoning for decisions
- Flag uncertainties
- Provide alternatives when blocked
- Never apologize (be professional)

## Terminal Command Patterns

**PowerShell Compatibility:**
- Always use `;` not `&&` for command chaining in PowerShell
- Use separate commands when chaining is complex
- Example: `cd project; npm install` not `cd project && npm install`

**Path Navigation:**
- Always check paths exist before navigating: `if (Test-Path "path") { Set-Location "path" }`
- Use `Join-Path` for path construction
- Use absolute paths from workspace root when possible

## File Editing Patterns

**When Files Are Blocked by Globalignore:**
- If file creation/editing is blocked (e.g., `.env.example`):
  1. Check if file exists and is gitignored
  2. Use `git update-index --no-assume-unchanged <file>` to temporarily unignore
  3. Make changes
  4. Use `git update-index --assume-unchanged <file>` to re-ignore if needed
  5. Alternative: Use programmatic file creation (Python/Node script) instead of direct editing

## Validation Schema Alignment

**When Creating Validation Schemas:**
- Always verify API request types match validation schema structure
- If API uses flat structure (e.g., `depth_min`, `depth_max`) but schema expects nested (e.g., `depth_range.min`), adapt schema to match API
- Test validation with actual API request objects before finalizing
- Document any structural differences between API types and validation schemas

## Dependency Management

**When Adding Testing Infrastructure:**
- Add all testing dependencies to package.json before writing tests
- Include: test framework, test utilities, type definitions, coverage tools
- Verify dependencies are installed before running tests
- Document required devDependencies in project setup instructions

## Feature Development: Research and Tutorials First

When developing a **new feature** (new system, new integration, or significant new capability): (1) **Research first** — use official docs and project docs (e.g. `docs/KNOWN_ERRORS.md`, tech-specific guides). (2) **Tutorials first** — follow relevant tutorials (official or version-specific) before customizing. (3) **Then expand** — extend or adapt only where the project explicitly needs something different. This is policy: research then tutorials then implement then expand.

## Error Recurrence Prevention

- **When an error occurs** (build, lint, test, or runtime): Clearly note what failed (command, file, or step), likely cause, and the fix applied. Do not continue as if the error was unimportant.
- **Where to record:** If the project has `docs/KNOWN_ERRORS.md` or `docs/LESSONS_LEARNED.md`, add a short entry there (error summary + fix + context). If not, state the error and fix in the response and suggest creating `docs/KNOWN_ERRORS.md` for future reference.
- **Before similar changes:** When about to make changes in the same area or of the same type, check `docs/KNOWN_ERRORS.md` or `docs/LESSONS_LEARNED.md` if present, and avoid repeating a previously documented error.

## Debug Mode

For **hard-to-reproduce or subtle bugs** (regressions, race conditions, performance issues), suggest **Cursor Debug Mode** (or the IDE's equivalent): the user reproduces the bug while the agent collects runtime data, instruments code with logging, generates hypotheses, and applies targeted fixes based on evidence. Do not guess; use instrumentation and hypotheses.

## AI Agent Checklist

- [ ] **Session log:** If project has one, read at task start and update at task end (see Session Continuity)
- [ ] Context gathered before changes
- [ ] Changes are complete
- [ ] Related files updated
- [ ] Code follows project patterns
- [ ] User informed of approach
- [ ] Errors encountered are noted and recorded; known errors checked before repeating similar work
- [ ] **Session cleanup done** — temporary scripts, result files, and diagnostic artifacts deleted (see Session Cleanup)
- [ ] Scripts that create resources are idempotent (check-before-create, no duplicates on re-run) where applicable
